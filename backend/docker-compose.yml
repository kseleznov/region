version: '3.8'
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: region
      POSTGRES_PASSWORD: region
      POSTGRES_DB: region
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
