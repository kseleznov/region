{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in */frontend/src/*.ts|*/frontend/src/*.tsx) (cd \"${f%%/frontend/*}/frontend\" && npx eslint --fix \"$f\") ;; esac; } >/tmp/claude-frontend-eslint-hook.log 2>&1 || true"
          }
        ]
      }
    ]
  }
}
